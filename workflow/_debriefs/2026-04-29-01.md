---
session-date: 2026-04-29
sequence: 1
first-commit: 8759d96
last-commit: 621ae33
duration: ~9 days (2026-04-24 → 2026-04-29)
---

# Session Debrief — 2026-04-29 #01

A large multi-day session covering the full build-out of the expense tracker: 18 features shipped through the workflow, plus a sustained post-launch polish pass fixing i18n gaps, display name writes, and insights UX — all driven by real-device testing feedback.

## Shipped

All 18 features moved to `done` during this session via direct merge:

- **001** `project-setup` — Firebase + Next.js scaffold with Google Sheets data layer
- **002** `expense-entry` — Mobile-first calculator with category picker and today's list
- **003** `category-system` — Default categories with emoji icons, archive/restore, reorder
- **004** `subscription-tracking` — Recurring expenses with auto-generated monthly/annual entries
- **005** `reports` — Monthly/annual summaries, donut/bar charts, category drill-down, payer breakdown
- **006** `auth-and-multi-user` — Google Sign-in restricted to two household emails
- **007** `bilingual-support` — Full EN/繁中 coverage across all surfaces
- **010** `edit-delete-expense` — Inline edit and delete from history detail sheet
- **011** `navigation` — Five-tab dock layout
- **012** `history` — Expense history with filter sheet, search, date presets
- **013** `history-filter` — Bottom sheet filter with presets, custom date range, search
- **014** `spending-insights` — On-demand AI insights via Claude API, tiered by data volume
- **017** `firebase-functions-data-persistence` — Cloud Functions API wiring all data to Sheets
- **019** `ui-fixes-round3` — Calculator, date headers, history sheet polish
- **020** `font-size-rescale` — 16/18/20px font size scale
- **021** `emoji-category-icons` — Replace Lucide icons with emoji on all surfaces
- **022** `sheets-alignment` — Users API endpoint, subscription naming fixes
- **026** `ui-polish-bugs` — Translation gaps (5 screens) and insights error UX fixed post real-device testing

## Filed (backlog)

- **025** `launch-test-plan` — Structured rollout from staging validation through production migration. Advanced to `build` (Phase 1 — Staging UAT in progress).

## Non-PR commits (workflow-only)

State transitions and scaffolding that don't belong to a PR:

- `fc81c13` refit: archive 18 done entities to `workflow/_archive`
- `d7ea141`, `5d7a57a` spec updates to 014 spending-insights and 025 launch-test-plan (UAT checklist in 繁中)
- `655e899` merge: 020 font-size-rescale — direct merge
- `8759d96` debrief: session 2026-04-24 #01

Post-launch fix commits (app code, all deployed):
- `523e860` display names to sheet, edit/delete in history, sort descending, i18n, insights errors
- `64a14aa` category picker locale, home header locale, donut Total translated
- `16bf05d` reports categories Chinese names; clean insights error UI
- `e4f24a0` date label: Today/Yesterday/Tomorrow + YYYY-MM-DD, others with day-of-week
- `d4f5376` new rows insert at top of sheet (DESC); subscriptions resolve paid_by display name
- `99da59a` subscriptions use logged-in user for paid_by; migrate-users endpoint
- `ea3107d` migrate-users path collision fix; remove duplicate date in calculator
- `5c134c2` user1/user2 → display names via LEGACY_USER_MAP; migration ran (25 expenses + 6 subscriptions fixed)
- `3371db6` user management, category management, settings sign-out fully translated
- `3a95347` structured insight errors (ai_error, data_error); 7-day threshold; Google Sheet link in settings
- `06625a6`, `621ae33`, `7b41f9b` README rewrite with personal motivation + demo video link

## Decisions

_(none recorded)_

## Issues — Workflow

- `paid_by`/`created_by` mismatch: client sent `user1`/`user2` but Users sheet stores emails as IDs — `resolveUserDisplayNames` never matched. Fixed with `LEGACY_USER_MAP` static fallback + one-time migration endpoint (25 expense rows + 6 subscription rows patched).
- Reports category names English-only: `reportService.ts` had its own partial `CATEGORY_META` map instead of using `DEFAULT_CATEGORIES` (which already had `name_zh`). Replaced entirely.
- Subscriptions hardcoded `paid_by: "user1"` — fixed to use `resolvedUserId` from auth context.

## Issues — Spacedock

None identified.

## Observations

_(none recorded)_

## What's Next

**Active:**
- `025` `launch-test-plan` — **Phase 1 (Staging UAT) in progress.** Captain testing now. Update `Current phase:` in the entity body as each phase completes.

**Ideation backlog:**
- `008` `data-migration` — needed for Phase 4 (migration dry run); build before Phase 4 starts
- `009` `multi-currency` — deferred, low priority
- `023` `product-packaging` — deferred
- `024` `themes-light-dark` — deferred
